Async Alembic environment for bot-tracker.

Создать миграцию:   alembic revision --autogenerate -m "описание"
Применить:           alembic upgrade head
Откатить на шаг:     alembic downgrade -1

URL и metadata берутся из app.config / app.db.models (env.py).
