services:
  bot:
    build: .
    image: bot-tracker:latest
    # UID/GID из .env (см. .env.example) — должны совпадать с id -u / id -g на хосте.
    user: "${UID:-1000}:${GID:-1000}"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
