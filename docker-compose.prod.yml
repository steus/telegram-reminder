# Прод-оверрайд (детально допиливается на этапе 7).
# Запуск:
#   docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build
services:
  bot:
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
